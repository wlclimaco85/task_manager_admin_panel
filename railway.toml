[build]
builder = "DOCKERFILE"
dockerfile = "Dockerfile"
buildArgs = { BACKEND_URL = "https://appacademia-production-be7e.up.railway.app" }

[deploy]
startCommand = ""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
